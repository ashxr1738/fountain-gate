import { cn } from "@/lib/utils";
export function StatusBadge({ status }: { status: string }) { const styles: Record<string, string> = { AVAILABLE: "bg-green-100 text-green-800", REQUESTED: "bg-amber-100 text-amber-800", CHECKED_OUT: "bg-blue-100 text-blue-800", PENDING: "bg-amber-100 text-amber-800", APPROVED: "bg-blue-100 text-blue-800", REJECTED: "bg-red-100 text-red-800", COMPLETED: "bg-green-100 text-green-800" }; return <span className={cn("inline-flex rounded-full px-2.5 py-1 text-xs font-bold", styles[status] || "bg-slate-100")}>{status.replaceAll("_", " ")}</span>; }
