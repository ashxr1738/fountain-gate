"use client";

import { useState } from "react";
import { saveAsset } from "@/app/(protected)/actions";
import { Button } from "@/components/ui/button";
import { AssetScanner } from "@/components/asset-scanner";
import type { Asset } from "@/lib/types";
export function AssetForm({ asset }: { asset?: Asset }) { const [assetCode, setAssetCode] = useState(asset?.asset_code || ""); return <form action={saveAsset} className="max-w-xl space-y-4 rounded-xl border bg-white p-5 shadow-sm"><input type="hidden" name="id" value={asset?.id}/><label className="block text-sm font-medium">Asset name<input required name="name" defaultValue={asset?.name} placeholder="Yamaha Keyboard" className="mt-1 w-full rounded-lg border p-3"/></label><label className="block text-sm font-medium">Asset code<input required name="asset_code" value={assetCode} onChange={event => setAssetCode(event.target.value.toUpperCase())} placeholder="KEY-001" className="mt-1 w-full rounded-lg border p-3 uppercase"/></label>{!asset && <div className="rounded-lg bg-slate-50 p-3"><p className="mb-2 text-sm text-slate-600">Have an existing equipment label?</p><AssetScanner onDetected={setAssetCode}/></div>}<label className="block text-sm font-medium">Category<input name="category" defaultValue={asset?.category || ""} placeholder="Musical Equipment" className="mt-1 w-full rounded-lg border p-3"/></label><label className="block text-sm font-medium">Description<textarea name="description" defaultValue={asset?.description || ""} placeholder="Main sanctuary keyboard" className="mt-1 min-h-24 w-full rounded-lg border p-3"/></label><Button className="w-full">{asset ? "Save changes" : "Add asset"}</Button></form>; }
